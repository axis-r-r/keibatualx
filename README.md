# バーチャル競馬 v1.1.0（Netlify）

## 今回の変更
- 起動時と再同期時にサーバー時刻を50回測定
- 1回ごとのRTT（通信往復時間）と推定時刻差を画面表示
- 50個の推定時刻差の中央値をゲーム時計の補正値として採用
- RTT中央値・補正中央値・全50件の履歴を確認可能
- 発走絶対時刻から30秒でゴール（描画遅延時も現在位置へ復帰）
- ゴール後15秒間は結果表示、発走45秒後から次走画面

Netlifyへフォルダーごとデプロイしてください。
